// Interactions are viewed within jurisdiction detail.
// This module is a placeholder for potential standalone interactions view.
export async function renderInteractions(el) {
  el.innerHTML = `<div class="empty">View interactions from the Entities tab</div>`;
}
